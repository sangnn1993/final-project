# server {
#     listen 80;
#     server_name fastinvest.cloud www.fastinvest.cloud;  # Thay bằng domain thật của bạn, thêm subdomain nếu cần

#     # Proxy tới frontend container (React/Vite SPA)
#     location / {
#         proxy_pass http://frontend:80/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # Tăng timeout nếu frontend load lâu (tùy chọn)
#         proxy_read_timeout 300;
#         proxy_connect_timeout 300;
#         proxy_send_timeout 300;
#     }

#     # Proxy tất cả request /api/* sang backend
#     location /api/ {
#         proxy_pass http://backend:5001/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # Cho phép CORS header từ backend (nếu backend chưa xử lý)
#         add_header 'Access-Control-Allow-Origin' '*' always;
#         add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
#         add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;

#         # Xử lý preflight OPTIONS ngay tại Nginx (rất hữu ích cho CORS)
#         if ($request_method = 'OPTIONS') {
#             add_header 'Access-Control-Allow-Origin' '*';
#             add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
#             add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
#             add_header 'Access-Control-Max-Age' 1728000;
#             add_header 'Content-Type' 'text/plain; charset=utf-8';
#             add_header 'Content-Length' 0;
#             return 204;
#         }
#     }

#     # Optional: Redirect www sang non-www (hoặc ngược lại)
#     # if ($host = 'www.fastinvest.cloud') {
#     #     return 301 https://fastinvest.cloud$request_uri;
#     # }

#     # Optional: Xử lý lỗi 502/504 đẹp hơn (tùy chọn)
#     error_page 502 503 504 /50x.html;
#     location = /50x.html {
#         root /usr/share/nginx/html;
#     }
# }